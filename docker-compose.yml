version: "3.3"
services:
  jupyterlab:
    build: .
    volumes:
      - .:/app
    command:  ["/usr/local/bin/jupyter",  "lab", "--ip=0.0.0.0", "--port=8888", "--allow-root", "--watch"]
    ports:
      - 8888:8888

volumes:
  project-volume: